# Phase 01 — Support laws (P0)

## Goal
Implement and test constitutional ID and error laws before any business logic.

## Acceptance gates
- ID-law tests pass (binary UUID sort + lock ordering + prefixed ids + order_ref)
- Error code registry tests pass
- Docs: explicit BAN language present
